import styled from '@emotion/styled'

/**
 * Button without the browser default styles
 */
export const UnstyledButton = styled.button({
  backgroundColor: 'transparent',
  border: 'none',
  padding: 0,
  margin: 0,
  color: 'inherit',
})
